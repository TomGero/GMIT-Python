f = open("data/iris.csv") # this opens the data files contained in the folder data/iris.csv

print (f) # this just prints the details of the folder

print(f.read()) # this prints the contents of the folder

f.close() # Important to close the folder when finished